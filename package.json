{
  "name": "mytourguide-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:provinces": "node scripts/generate-province-pages.mjs",
    "prebuild": "npm run generate:provinces",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
